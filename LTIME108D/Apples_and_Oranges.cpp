#include <bits/stdc++.h>
using namespace std;

int main()
{
    int x;
    cin >> x;
    int a, b;
    cin >> a >> b;
    if (a + b <= x)
    {
        cout << "Yes" << endl;
    }
    // if (a+b<=x)
    else
    {
        cout << "No" << endl;
    }

    return 0;
}